
enum LightMode {
    SIMPLE, //just on
    BLINK_FAST,
    BLINK_SLOW,
    CYCLE_FAST,
    CYCLE_SLOW,
    PULSE_ON_OFF_SHORT, //short pulse then turns off
    PULSE_ON_OFF_LONG //long pulse then turns off
};
